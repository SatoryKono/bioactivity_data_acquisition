# Install the project in editable mode together with all development extras.
# This ensures mandatory runtime dependencies such as pyarrow are available
# when running the test-suite locally or in CI.
-e .[dev]
