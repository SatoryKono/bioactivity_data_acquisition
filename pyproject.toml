[project]
name = "bioactivity-data-acquisition"
version = "0.1.0"
description = "Common CLI interface for bioactivity data pipelines"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.6.0",
    "typer>=0.9.0",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
addopts = "-q"
