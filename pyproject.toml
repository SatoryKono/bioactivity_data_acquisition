[project]
name = "bioactivity-data-acquisition"
version = "0.1.0"
description = "Bioactivity data acquisition utilities"
authors = [{ name = "AutoGenerated" }]
requires-python = ">=3.10"
dependencies = [
  "requests>=2.31",
  "structlog>=24.1",
  "backoff>=2.2",
]

[project.optional-dependencies]
test = [
  "pytest>=7.4",
  "responses>=0.25",
]

[build-system]
requires = ["setuptools>=65"]
build-backend = "setuptools.build_meta"
