> Scope:
> - USE WHEN naming pipelines; {entity}_{source} in lowercase
> - Use when editing files matching: `src/**`, `docs/pipelines/**/*.md`, `configs/**/*.yaml`
# MANDATORY
- Pipeline identifiers are named `{entity}_{source}` (lowercase). Entity first, then source.

# EXAMPLES
- `activity_chembl`, `target_uniprot`, `document_pubmed`

# REFERENCE
See ../../docs/styleguide/08-etl-architecture.md
