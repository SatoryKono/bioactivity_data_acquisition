# Bioactivity Data Acquisition - Environment Variables Template
# Copy this file to .env and fill in your actual values

# =============================================================================
# API KEYS (Required for enhanced rate limits and access)
# =============================================================================

# ChEMBL API Token (optional, but recommended for higher rate limits)
# Get from: https://chembl.gitbook.io/chembl-interface-documentation/web-services/chembl-data-web-services
CHEMBL_API_TOKEN=your_chembl_token_here

# PubMed API Key (optional, but recommended for higher rate limits)
# Get from: https://ncbiinsights.ncbi.nlm.nih.gov/2017/11/02/new-api-keys-for-the-e-utilities/
PUBMED_API_KEY=your_pubmed_key_here

# Semantic Scholar API Key (optional, but recommended for higher rate limits)
# Get from: https://www.semanticscholar.org/product/api
SEMANTIC_SCHOLAR_API_KEY=your_semantic_scholar_key_here

# Crossref API Key (optional, but recommended for higher rate limits)
# Get from: https://www.crossref.org/requestaccount/
CROSSREF_API_KEY=your_crossref_key_here

# =============================================================================
# GLOBAL CONFIGURATION OVERRIDES
# =============================================================================

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
BIOACTIVITY__LOGGING__LEVEL=INFO

# HTTP global timeout in seconds
BIOACTIVITY__HTTP__GLOBAL__TIMEOUT_SEC=30

# Number of worker threads for parallel processing
BIOACTIVITY__RUNTIME__WORKERS=4

# Limit number of records processed (null for no limit)
BIOACTIVITY__RUNTIME__LIMIT=null

# Enable strict validation mode
BIOACTIVITY__VALIDATION__STRICT=true

# =============================================================================
# TELEMETRY AND MONITORING (Optional)
# =============================================================================

# Jaeger endpoint for distributed tracing
# Format: http://localhost:14268/api/traces
JAEGER_ENDPOINT=

# Jaeger agent host (default: localhost)
JAEGER_AGENT_HOST=localhost

# Jaeger agent port (default: 6831)
JAEGER_AGENT_PORT=6831

# Service version for telemetry
SERVICE_VERSION=0.1.0

# Deployment environment (development, staging, production)
ENVIRONMENT=development

# =============================================================================
# DEVELOPMENT AND TESTING (Optional)
# =============================================================================

# Enable debug mode for development
DEBUG=false

# Test data directory (for integration tests)
TEST_DATA_DIR=tests/fixtures

# Benchmark results directory
BENCHMARK_RESULTS_DIR=benchmark_results

# =============================================================================
# SECURITY NOTES
# =============================================================================
# 
# 1. Never commit the actual .env file to version control
# 2. Use strong, unique API keys for each service
# 3. Rotate API keys regularly (every 90 days recommended)
# 4. Use environment-specific values (dev/staging/prod)
# 5. Consider using a secrets management service for production
#
# =============================================================================
