# Copy this file to `.env` and replace placeholder values with valid credentials.
# Shell comments explain why each variable is required and which format to use.

# -----------------------------------------------------------------------------
# PubMed E-utilities configuration
# -----------------------------------------------------------------------------
# Unique identifier reported to NCBI in the `tool` query parameter.
PUBMED_TOOL=bioetl-document-pipeline
# Contact email that NCBI can use for quota notifications.
PUBMED_EMAIL=821311@gmail.com
# Optional API key that increases PubMed rate limits; leave empty to use public quotas.
PUBMED_API_KEY=

# -----------------------------------------------------------------------------
# Crossref polite pool configuration
# -----------------------------------------------------------------------------
# Email address advertised in the `mailto` query parameter for polite Crossref access.
CROSSREF_MAILTO=821311@gmail.com

# -----------------------------------------------------------------------------
# Semantic Scholar access token
# -----------------------------------------------------------------------------
# Required for production usage to unlock the full Semantic Scholar rate limits.
SEMANTIC_SCHOLAR_API_KEY=o2N1y1RHYU3aqEj556Oyv4oBzZrHthM2bWda2lf4

# -----------------------------------------------------------------------------
# IUPHAR Guide to Pharmacology API token
# -----------------------------------------------------------------------------
# Mandatory for the target enrichment pipeline; requests without this key are rejected.
IUPHAR_API_KEY=
