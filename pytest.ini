[pytest]
markers =
    unit: Unit tests (fast, no IO/network)
    determinism: Determinism checks
    integration: Integration tests
    slow: Slow tests
testpaths = tests/core tests/infrastructure tests/pipelines tests/golden tests/cli
pythonpath = src
norecursedirs = ~src
